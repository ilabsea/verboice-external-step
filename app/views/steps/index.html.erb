<table class="table table-hover">
	<caption><strong>Step List</strong></caption>
	<thead>
		<tr>
			<th>Name</th>
			<th>Description</th>
			<th>Url</th>
		</tr>
	</thead>
	<tbody>
		<% @steps.each do |step| %>
			<tr>
				<td><%= link_to step.display_name, edit_step_path(step.name) %></td>
				<td><%= step.description %></td>
				<td><%= link_to step.url, step.url %></td>
			</tr>
		<% end %>
	</tbody>
</table>
